claude-full-learning deployed as release "{{ .Release.Name }}" in namespace "{{ include "claude-full-learning.namespace" . }}".

Check rollout status:
  kubectl rollout status deployment/{{ include "claude-full-learning.fullname" . }} -n {{ include "claude-full-learning.namespace" . }}

Reach the app:
{{- if .Values.ingress.enabled }}
  Add "$(minikube ip)  {{ .Values.ingress.host }}" to /etc/hosts, then:
  curl http://{{ .Values.ingress.host }}/actuator/health
{{- else }}
  minikube service {{ include "claude-full-learning.fullname" . }} -n {{ include "claude-full-learning.namespace" . }} --url
{{- end }}

See docs/deployment.md for the full walkthrough, docs/rollback.md if this deploy needs undoing, and
docs/troubleshooting.md for common failure modes (ImagePullBackOff, HPA <unknown>, etc).
