# Dev overlay: single replica, Ingress on for exercising host-based routing locally, verbose logs.
namespace:
  name: java-spring-auth-service-claude-dev

replicaCount: 1

ingress:
  enabled: true
  host: java-spring-auth-service-claude.dev.local

config:
  logLevel: DEBUG

autoscaling:
  enabled: false
