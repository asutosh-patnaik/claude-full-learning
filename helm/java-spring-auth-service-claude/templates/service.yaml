apiVersion: v1
kind: Service
metadata:
  name: {{ include "java-spring-auth-service-claude.fullname" . }}
  namespace: {{ include "java-spring-auth-service-claude.namespace" . }}
  labels:
    {{- include "java-spring-auth-service-claude.labels" . | nindent 4 }}
    app.kubernetes.io/component: app
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "java-spring-auth-service-claude.selectorLabels" . | nindent 4 }}
    app.kubernetes.io/component: app
  ports:
    # Named (not just numeric) because the ServiceMonitor (observability, off by default) scrapes
    # by port name, not number.
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
